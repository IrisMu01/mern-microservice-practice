/.idea

# .env is not separated between different microservices because they connect to the same db/redis/rabbitMQ servers
main/back-end/.env
main/front-end/.env

# scripts/files for creating docker secrets
main/secrets/files
main/secrets/swarm-scripts/secrets.sh
main/secrets/swarm-scripts/secrets.ps1

# contains aws api calls with credentials
main/scripts/boto3
